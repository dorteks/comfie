import React from "react";
import { Box } from "@chakra-ui/react";
import LandingPage from "@/container/landingPage";

const Home = () => {
  return (
    <Box>
      <LandingPage />
    </Box>
  );
};

export default Home;
